bon je vais lister ici les modifications globales à faire dans l'application

-- déja il faut retirer toutes les icones actuelles pour les remplacer par des lucid ou tout autre équivalent
-- repenser l'architecture globale de l'application pour corriger les problèmes qui ont été soulignés 
-- séparer le fichier index.css qui est beaucoup trop long 
-- penser au fait que l'ia sera intégrer plus tard 
-- construire le document qui résume l'ensemble des règles ABAC qui seront implémentés dans l'application
-- vérifier que les wireframes ont bien été respecté de long en large 
-- la database de l'application est en désordre la mettre à jour pour qu'elle puissse correspondre à notre système final
-- c'est vrai normalement l'application est censé se lancer sur la splash screen et pas directement sur la page de login supprimer les boutons qui sont écris acceuil et présentation sur la login page 
-- les onboarding sont lancés uniquement à la première connexion sur l'appareil (déterminer comment bien implémenter ce système)
-- je veux aussi ajouter un tutoriel pour permettre aux utilisateurs de comprendre comment focntionne l'application (il est accessible uniquement lors de la première connexion)
-- ajouter la documentation de l'application dans les paramètres en fonction du role 
-- le bouton se souvenir de moi fonctionne comment dans le code 
-- il faut que j'améliore l'animation sur la login page 
-- le formulaire de création de compte est touffu l'améliorer en le divisant étape par étape 
-- a quoi servent les configurations vite.config.js ?
-- comment changer la clé secret de mon jwt et pourquoi ?